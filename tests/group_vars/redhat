# TODO: collectd 4.10 is available for CentOS 6 only
versions:
  collectd: 5.8.1

collectd:
  providers:
    pkg:
      prerequisites:
        - epel-release
      packages:
        - "collectd-{{ versions.collectd }}"
        - "collectd-netlink-{{ versions.collectd }}"
